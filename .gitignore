# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example

# Hardhat
artifacts/
cache/
typechain-types/
coverage/
coverage.json

# Foundry (если позже добавим fuzz через forge)
out/
forge-cache/
broadcast/

# Logs / runtime
logs/
*.log

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# agent-service local DB
agent-service/*.sqlite
agent-service/*.db
agent-service/data/

# Context handoff & personal notes — local only, not for the public repo
HANDOFF.md
/промпт.txt

# Design source material — large (ShinyPokerDesign ~52MB), not deployed
/New design ShinyLuck final/
/ShinyLuckDesign/
/ShinyPokerDesign/

# demo-bot wallets
demo-bot/wallets.json

# Local runtime state (per-user strategies, HM cron state)
data/
**/hm-state.json

# Tooling working directories — never commit these
.claude/
slots-from-design/

# Trailer / demo-video capture assets (local-only video tooling, incl. a
# Python venv under trailer/aigen/venv — must never be committed).
trailer/
_trailer_v13_backup/
frontend/trailer.html
frontend/lib/trailer-sim.js

# Design scratch (integrated copy lives in frontend/ after wiring)
Roulettedesign/

# Local diagnostic / scratch scripts (underscore-prefixed, not shipped)
scripts/_diag-*.js
scripts/_q.js
scripts/_rtp.js

# Personal working files (hackathon submission drafts, notes, etc.)
SUBMISSION.md
NOTES.md
TODO.md

# seeds revealed off-chain — must NEVER be committed
deployments/seeds/

# generated copy of the deployment manifest for the static frontend.
# Excluded for hardhat local runs (they overwrite with chainId 31337); the
# testnet manifest IS committed because Vercel needs it served as a static
# asset at /deployments/somniaTestnet.json.
frontend/deployments/hardhat.json
frontend/deployments/localhost.json


# Dependencies
node_modules/
agent-service/node_modules/
frontend/node_modules/
subgraph/node_modules/

# Environment files (CRITICAL — never commit private keys)
.env
.env.local
.env.*.local
agent-service/.env
**/.env

# Hardhat
cache/
artifacts/
typechain-types/

# Build outputs
dist/
build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pm2.log

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Seeds (содержат revealed server seeds — НЕ коммитить)
deployments/seeds/

# Subgraph build
subgraph/build/
subgraph/generated/

# Test coverage
coverage/
coverage.json

# Temp seeds-b64 dump for Railway env-var pasting
seeds_b64_gen10.txt
seeds_b64_*.txt

scripts/_q*.js

# ShinyPoker design source videos (52MB, not needed in repo)
ShinyPokerDesign/uploads/
deployments/zk-active/

# Local pre-refactor backups (never for the repo)
_zkshuffle-backup-*/

# NOTE: frontend/poker/*.compiled.js used to be ignored, back when they were only
# a syntax check and the site shipped raw .jsx to be transpiled in the browser.
# The pages load the compiled files now, so they are the deployed artifact and
# have to live in the repo — regenerate with `node scripts/build-poker-jsx.js`
# after editing any poker .jsx.

# Screenshots the user drops in for Claude to look at — local only
/images for claude/

# X bot account credentials (login:password:email:email_pass:token:2FA).
# Never commit: this is a public repo. Used once to seed the scraper pool ON
# THE VPS, never from a local machine.
Accounts.txt
accounts*.txt
!*.example.txt

# Raw samples written by scripts/_probe-latency.js — a measurement artefact,
# regenerated on every run. The numbers that matter live in HANDOFF.md.
scripts/probe-out.json

# browser E2E run artifacts (screenshots + the throwaway local deployment)
test/_e2e-shots/
deployments/poker-localhost.json

# Table music · 12 tracks cut from the owner's lounge mix (~180 MB of mp3).
# Deployed straight to the VPS (frontend/assets/music on /root/poker-web), not
# carried in git: binaries this size make every clone and every diff worse, and
# they never change. Re-cut them with scripts/_cut-music.sh if they are ever lost.
frontend/assets/music/
test/_shots-mobile/
